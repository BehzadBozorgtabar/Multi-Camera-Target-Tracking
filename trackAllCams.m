
%% 1
clear; 
close all; 
config; 
setup;
configFile   = 'camera1.config';
dataset      = loadExperimentSetting(configFile, dataset);

main

%% 2
clear; 
close all; 
config; 
setup;
configFile   = 'camera2.config';
dataset      = loadExperimentSetting(configFile, dataset);

main

%% 3
clear; 
close all; 
config; 
setup;
configFile   = 'camera3.config';
dataset      = loadSetting(configFile, dataset);

main

%% 4 
clear; 
close all; 
config; 
setup;
configFile   = 'camera4.config';
dataset      = loadSetting(configFile, dataset);

main

%% 5
clear; 
close all; 
config; 
setup;
configFile   = 'camera5.config';
dataset      = loadSetting(configFile, dataset);

main

%% 6
clear; 
close all; 
config; 
setup;
configFile   = 'camera6.config';
dataset      = loadSetting(configFile, dataset);

main

%% 7
clear; 
close all; 
config; 
setup;
configFile   = 'camera7.config';
dataset      = loadSetting(configFile, dataset);

main

%% 8
clear; 
close all; 
config; 
setup;
configFile   = 'camera8.config';
dataset      = loadSetting(configFile, dataset);

main
